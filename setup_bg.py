from setuptools import setup, find_namespace_packages

setup(name='batchgenerators',
      packages=find_namespace_packages(include=["batchgenerators", "batchgenerators.*"]),
      version='0.21',
      install_requires=[
      ],
     
      
      )
